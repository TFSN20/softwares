1. mklink 文档 下载 图片 视频 到 D盘
2. 创建还原点
- 显示器，选择仅仅显示2屏幕，1600*900 60hz，

## 公司电脑
- 关闭最近使用的文件，常用的文件夹，设置里关闭搜索建议和云搜索

## 系统禁止更新
- 家庭版启用策略
  ```
  @echo off
  pushd "%~dp0"
  dir /b %systemroot%\Windows\servicing\Packages\Microsoft-Windows-GroupPolicy-ClientExtensions-Package~3*.mum >gp.txt
  dir /b %systemroot%\servicing\Packages\Microsoft-Windows-GroupPolicy-ClientTools-Package~3*.mum >>gp.txt
  for /f %%i in ('findstr /i . gp.txt 2^>nul') do dism /online /norestart /add-package:"%systemroot%\servicing\Packages\%%i"
  pause
  ```
- win+r
  ```
  gpedit.msc
  ```
- 依次进入
  ```
  计算机配置
  → 管理模板
  → Windows 组件
  → Windows 更新
  → 管理最终用户体验
  ```
  配置自动更新改为禁用
- 注册表方法(保存到`2099.reg`双击运行)
  ```
  Windows Registry Editor Version 5.00
  
  [HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\WindowsUpdate\UX\Settings]
  "FlightSettingsMaxPauseDays"=dword:00001b58
  "PauseFeatureUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseFeatureUpdatesEndTime"="2099-12-31T23:59:59Z"
  "PauseQualityUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseQualityUpdatesEndTime"="2099-12-31T23:59:59Z"
  "PauseUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseUpdatesExpiryTime"="2099-12-31T23:59:59Z"
  ```
## 鼠标定位圆圈
保存为`鼠标定位圆圈.bat`双击运行
```
@echo off
:: 设置编码为 UTF-8 防止中文乱码
chcp 65001 >nul
cls

echo ==================================================
echo   Windows 鼠标定位设置（按下 Ctrl 显示圆圈）
echo ==================================================
echo   [1] 开启该功能
echo   [2] 关闭该功能 
echo   [3] 退出
echo ==================================================
set /p choice="请输入数字 (1-3) 并按回车: "

if "%choice%"=="1" goto ENABLE
if "%choice%"=="2" goto DISABLE
if "%choice%"=="3" goto END
goto INVALID

:ENABLE
set "status=$true"
set "msg=已成功【启用】按下 Ctrl 键显示鼠标位置功能！"
goto PROCESS

:DISABLE
set "status=$false"
set "msg=已成功【关闭】按下 Ctrl 键显示鼠标位置功能！"
goto PROCESS

:PROCESS
:: 创建临时 PowerShell 脚本以规避批处理转义字符问题
set "temp_ps=%temp%\sonar_settings.ps1"
echo $code = @' > "%temp_ps%"
echo using System.Runtime.InteropServices; >> "%temp_ps%"
echo public class MouseSonar { >> "%temp_ps%"
echo     [DllImport("user32.dll")] >> "%temp_ps%"
echo     public static extern int SystemParametersInfo(uint u, uint up, bool lpv, uint fu); >> "%temp_ps%"
echo } >> "%temp_ps%"
echo '@ >> "%temp_ps%"
echo Add-Type -TypeDefinition $code >> "%temp_ps%"
echo [MouseSonar]::SystemParametersInfo(0x101D, 0, %status%, 3) >> "%temp_ps%"

:: 执行并清理临时文件
powershell -NoProfile -ExecutionPolicy Bypass -File "%temp_ps%"
del "%temp_ps%"

echo.
echo %msg%
echo.
pause
goto END

:INVALID
echo 输入无效，请重新运行脚本。
pause
goto END

:END
exit
```
## 设置网络DNS服务器地址
保存为`SetDNS.bat`双击运行
```
@echo off
:: 设置编码为 UTF-8 防止中文乱码
chcp 65001 >nul
cls
setlocal enabledelayedexpansion

:: 检查并自动获取管理员权限
>nul 2>&1 "%SYSTEMROOT%\system32\cacls.exe" "%SYSTEMROOT%\system32\config\system"
if '%errorlevel%' NEQ '0' (
    echo 正在请求管理员权限...
    goto UACPrompt
) else ( goto gotAdmin )

:UACPrompt
    echo Set UAC = CreateObject^("Shell.Application"^) > "%temp%\getadmin.vbs"
    echo UAC.ShellExecute "%~s0", "", "", "runas", 1 >> "%temp%\getadmin.vbs"
    "%temp%\getadmin.vbs"
    exit /B

:gotAdmin
    if exist "%temp%\getadmin.vbs" ( del "%temp%\getadmin.vbs" )
    pushd "%CD%"
    CD /D "%~dp0"

:menu
cls
echo ===================================================
echo             Windows 一键设置 DNS 工具
echo ===================================================
echo  [1] 阿里云 DNS (223.5.5.5 / 223.6.6.6)
echo  [2] 腾讯云 DNS (119.29.29.29 / 119.28.28.28)
echo  [3] 百度云 DNS (180.76.76.76 / 180.76.76.77)
echo  [4] 114 DNS (114.114.114.114 / 114.114.115.115)
echo  [5] 谷歌 DNS (8.8.8.8 / 8.8.4.4)
echo  [6] Cloudflare DNS (1.1.1.1 / 1.0.0.1)
echo  [7] 手动输入自定义 DNS
echo  [8] 恢复自动获取 DNS (DHCP)
echo  [9] 退出
echo ===================================================
set /p choice="请输入选项 [1-9]: "

if "%choice%"=="1" (
    set "DNS1=223.5.5.5"
    set "DNS2=223.6.6.6"
    goto SelectAdapter
)
if "%choice%"=="2" (
    set "DNS1=119.29.29.29"
    set "DNS2=119.28.28.28"
    goto SelectAdapter
)
if "%choice%"=="3" (
    set "DNS1=180.76.76.76"
    set "DNS2=180.76.76.77"
    goto SelectAdapter
)
if "%choice%"=="4" (
    set "DNS1=114.114.114.114"
    set "DNS2=114.114.115.115"
    goto SelectAdapter
)
if "%choice%"=="5" (
    set "DNS1=8.8.8.8"
    set "DNS2=8.8.4.4"
    goto SelectAdapter
)
if "%choice%"=="6" (
    set "DNS1=1.1.1.1"
    set "DNS2=1.0.0.1"
    goto SelectAdapter
)
if "%choice%"=="7" (
    set /p DNS1="请输入首选 DNS: "
    set /p DNS2="请输入备用 DNS (若无备用请直接回车): "
    goto SelectAdapter
)
if "%choice%"=="8" (
    goto SelectAdapter
)
if "%choice%"=="9" (
    exit
)
echo 输入错误，请重新输入！
pause
goto menu

:SelectAdapter
cls
echo 正在扫描并分析系统中的网卡及当前 DNS 配置，请稍候...
set "count=0"

:: [新增：一键获取当前所有网卡的 DNS 映射关系，防止多语言环境乱码]
for /f "tokens=1* delims=:" %%i in ('powershell -NoProfile -Command "Get-DnsClientServerAddress -AddressFamily IPv4 | ForEach-Object { $_.InterfaceAlias + ':' + ($_.ServerAddresses -join ', ') }"') do (
    set "alias=%%i"
    set "ips=%%j"
    if not defined ips (
        set "dns_map[!alias!]=自动获取 (或未分配)"
    ) else (
        set "dns_map[!alias!]=!ips!"
    )
    set "ips="
)

:: 1. 扫描并优先添加：已连接的物理网卡 (推荐级最高)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    if /I "!state!"=="connected" (
        echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            set "adapter_state[!count!]=已连接"
        )
    )
)

:: 2. 扫描并添加：未连接的物理网卡 (推荐级次之)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    if /I "!state!"=="disconnected" (
        echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            set "adapter_state[!count!]=未连接"
        )
    )
)

:: 3. 扫描并添加：虚拟网卡 (推荐级最低)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
    if !errorlevel! equ 0 (
        :: 过滤掉没有修改价值的 Loopback 网卡
        echo !name! | findstr /I /C:"Loopback" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            if /I "!state!"=="connected" (
                set "adapter_state[!count!]=已连接 (虚拟)"
            ) else (
                set "adapter_state[!count!]=未连接 (虚拟)"
            )
        )
    )
)

if "%count%"=="0" (
    echo [错误] 未在系统中检测到有效的网络适配器！
    pause
    goto menu
)

:ShowChoice
cls
echo ===================================================
echo                请选择要应用 DNS 的网卡
echo ===================================================
echo  [A] 应用到所有当前“已连接”的网卡 (推荐)
echo ---------------------------------------------------
for /L %%g in (1,1,%count%) do (
    set "ad_name=!adapter[%%g]!"
    :: 使用临时变量解析嵌套延迟变量，从而正确显示各网卡的当前 DNS
    for /f "delims=" %%v in ("dns_map[!ad_name!]") do (
        echo  [%%g] !ad_name!  ^(!adapter_state[%%g]!^)
        echo      当前 DNS: !%%v!
    )
    echo ---------------------------------------------------
)
echo ===================================================
set /p adapter_choice="请输入选项 [A 或数字 1-%count%]: "

if /I "%adapter_choice%"=="A" (
    set "apply_mode=all_active"
    goto ExecuteApply
)

if not defined adapter[%adapter_choice%] (
    echo [错误] 输入的选择无效，按任意键重新选择...
    pause >nul
    goto ShowChoice
)

set "apply_mode=single"
set "selected_adapter=!adapter[%adapter_choice%]!"
goto ExecuteApply

:ExecuteApply
cls
echo 正在配置 DNS，请稍候...
echo.

if "%apply_mode%"=="single" (
    call :SetDNS "%selected_adapter%"
) else (
    set "applied_any=0"
    for /L %%g in (1,1,%count%) do (
        :: 仅对状态为“已连接”的网卡进行操作
        if "!adapter_state[%%g]!"=="已连接" (
            call :SetDNS "!adapter[%%g]!"
            set "applied_any=1"
        )
    )
    if "!applied_any!"=="0" (
        echo [提示] 未发现任何处于“已连接”状态的网卡，无法批量应用。
    )
)

echo.
echo 正在刷新 DNS 缓存...
ipconfig /flushdns
echo.
echo DNS 配置流程已结束。
pause
goto menu

:SetDNS
set "adapterName=%~1"
echo 正在配置网卡 [%adapterName%]...

if "%choice%"=="8" (
    netsh interface ipv4 set dnsservers name="%adapterName%" source=dhcp >nul 2>&1
    if !errorlevel! equ 0 (
        echo [成功] 网卡 [%adapterName%] 已恢复为自动获取 DNS。
    ) else (
        echo [失败] 网卡 [%adapterName%] 恢复失败，请检查名称是否包含特殊字符。
    )
) else (
    netsh interface ipv4 set dnsservers name="%adapterName%" source=static address=%DNS1% register=primary >nul 2>&1
    if !errorlevel! equ 0 (
        echo [成功] 网卡 [%adapterName%] 首选 DNS 已设置为: %DNS1%
        if not "%DNS2%"=="" (
            netsh interface ipv4 add dnsservers name="%adapterName%" address=%DNS2% index=2 >nul 2>&1
            echo [成功] 网卡 [%adapterName%] 备用 DNS 已设置为: %DNS2%
        )
    ) else (
        echo [失败] 网卡 [%adapterName%] DNS 设置失败。
    )
)
goto :eof
```
## 关闭 了解有关此图片的信息 桌面图标
保存为`隐藏图标.bat`双击运行
```
@echo off
:: 使用UTF-8编码，防止CMD窗口中文显示乱码
chcp 65001 >nul

echo 正在修改注册表以隐藏“了解有关此图片的信息”图标...
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\HideDesktopIcons\NewStartPanel" /v "{2cc5ca98-6485-489a-920e-b3e88a6ccce3}" /t REG_DWORD /d 1 /f

echo 正在重启资源管理器使设置生效...
taskkill /f /im explorer.exe
start explorer.exe

echo 隐藏操作已完成！
pause
```
## 设置xlsx文件的默认字体、大小
保存为`set_excel_custom_font.bat`双击运行（保存时编码请选择“ANSI”），默认为微软雅黑9号
```
@echo off
:: 强制使用 GBK 编码以避免中文字符乱码
chcp 936 >nul
title Excel 默认字体和字号修改工具

:: 检测是否是管理员权限运行的第二阶段
if "%~1"=="run_admin" goto :AdminStart

:: ======================================================
:: 用户交互输入：选择字体和字号
:: ======================================================
echo ======================================================
echo               Excel 默认字体/字号修改工具
echo ======================================================
echo.

set "font_name=微软雅黑"
set /p "user_font=请输入想要设置的字体名称（如：宋体、微软雅黑、Arial）[直接回车默认为 微软雅黑]: "
if not "%user_font%"=="" set "font_name=%user_font%"

set "font_size=9"
set /p "user_size=请输入想要设置的字号大小（如：9、10、11、12）[直接回车默认为 9]: "
if not "%user_size%"=="" set "font_size=%user_size%"

:: ======================================================
:: 第一部分：修改注册表（解决在Excel软件内点击“新建”时的默认字体）
:: ======================================================
echo.
echo 正在设置 Excel 软件内新建文件的默认配置为: %font_name% (%font_size%号)...
:: 适用于 Office 2016 及更高版本 / Office 365
reg add "HKEY_CURRENT_USER\Software\Microsoft\Office\16.0\Excel\Options" /v "Font" /t REG_SZ /d "%font_name%,%font_size%" /f >nul 2>&1
:: 适用于 Office 2013
reg add "HKEY_CURRENT_USER\Software\Microsoft\Office\15.0\Excel\Options" /v "Font" /t REG_SZ /d "%font_name%,%font_size%" /f >nul 2>&1
echo 注册表修改成功！
echo ======================================================

:: ======================================================
:: 第二部分：询问是否修改右键“新建工作表”的模板
:: ======================================================
echo.
echo 提示：
echo 桌面或文件夹“右键 - 新建 - Microsoft Excel 工作表”是由系统独立模板决定的。
echo 刚才修改的注册表只对“软件内新建空白工作簿”有效。
echo 是否需要一并修改右键新建的默认模板？
set "choice=Y"
set /p choice="请输入 Y（需要） 或 N（不需要）并回车 [直接回车默认为 Y]: "

if /i "%choice%"=="Y" (
    goto :RequestAdmin
) else (
    echo 操作已结束。请重启电脑或 Excel 软件查看效果。
    pause
    exit /b
)

:RequestAdmin
:: 检查当前是否已经是管理员权限
net session >nul 2>&1
if %errorLevel% == 0 (
    goto :AdminStart
) else (
    echo 正在请求管理员权限以修改系统模板文件...
    :: 【已修正】移除了引发 CMD 冲突的转义双引号，直接将变量作为独立参数传递
    powershell -Command "Start-Process '%~dpnx0' -ArgumentList 'run_admin', '%font_name%', '%font_size%' -Verb RunAs"
    exit /b
)

:AdminStart
chcp 936 >nul
:: 如果是从管理员模式启动的，从参数中还原用户输入的变量
if "%~1"=="run_admin" (
    set "font_name=%~2"
    set "font_size=%~3"
)

:: 防呆保护，确保变量不为空
if "%font_name%"=="" set "font_name=微软雅黑"
if "%font_size%"=="" set "font_size=9"

echo ======================================================
echo 正在定位 Excel 右键新建模板 (EXCEL12.xlsx) 的位置...

:: 查找可能存在 EXCEL12.xlsx 模板的路径
set "template_found="

:: 路径1: 64位Office (默认安装路径)
if exist "C:\Program Files\Microsoft Office\root\vfs\Windows\SHELLNEW\EXCEL12.xlsx" (
    set "target_dir=C:\Program Files\Microsoft Office\root\vfs\Windows\SHELLNEW"
    set "template_found=1"
)
:: 路径2: 32位Office安装在64位系统
if not defined template_found (
    if exist "C:\Program Files (x86)\Microsoft Office\root\vfs\Windows\SHELLNEW\EXCEL12.xlsx" (
        set "target_dir=C:\Program Files (x86)\Microsoft Office\root\vfs\Windows\SHELLNEW"
        set "template_found=1"
    )
)
:: 路径3: 部分系统直接在 Windows\ShellNew
if not defined template_found (
    if exist "C:\Windows\ShellNew\EXCEL12.xlsx" (
        set "target_dir=C:\Windows\ShellNew"
        set "template_found=1"
    )
)

if not defined template_found (
    echo [警告] 未能在常见路径中找到右键新建的 EXCEL12.xlsx 模板文件。
    echo 软件内的“空白工作簿”已修改成功，但右键新建模板需要您手动修改。
    pause
    exit /b
)

echo 已找到目标路径: %target_dir%
echo 正在备份原模板文件为 EXCEL12.xlsx.bak...
copy /y "%target_dir%\EXCEL12.xlsx" "%target_dir%\EXCEL12.xlsx.bak" >nul

echo 正在通过 PowerShell 调用 Excel 生成全新的“%font_name% %font_size%号”模板...
:: 利用 PowerShell COM 调用 Excel 生成设置好目标字体的模板，并保存覆盖
powershell -Command "$excel = New-Object -ComObject Excel.Application; $excel.Visible = $false; $excel.DisplayAlerts = $false; $wb = $excel.Workbooks.Add(); $style = $wb.Styles.Item('Normal'); $style.Font.Name = '%font_name%'; $style.Font.Size = %font_size%; $wb.SaveAs('%target_dir%\EXCEL12.xlsx'); $wb.Close(); $excel.Quit();"

if %errorLevel% == 0 (
    echo.
    echo [成功] 右键新建 Excel 工作表的默认模板修改完成（%font_name% %font_size%号）！
    echo 现在不管是“在软件内新建”还是“右键新建”，默认配置均已生效。
) else (
    echo.
    echo [失败] 生成模板时出错，可能由于 Excel 正在运行或权限受限，请关闭所有 Excel 窗口后再试。
)

pause
```
## 设置word默认字体和大小
保存为`SetWordFont.bat`双击运行
```
@echo off
chcp 65001 >nul
title 设置 Word 默认字体 (中西文独立 / 大小映射) 

:: 自动申请管理员权限
>nul 2>&1 "%SYSTEMROOT%\system32\cacls.exe" "%SYSTEMROOT%\system32\config\system"
if '%errorlevel%' NEQ '0' (
    echo 正在请求管理员权限... 
    powershell -Command "Start-Process '%~f0' -Verb RunAs"
    exit /b
)

cd /d "%~dp0"

echo ========================================================
echo     Word 默认字体修改工具 (完美兼容 Office C2R/VFS) 
echo ========================================================
echo.

:: 1. 交互式选择字体和大小，使用 if not defined 防止特殊符号导致闪退
set "CHINESE_FONT="
set /p "CHINESE_FONT=请输入中文字体名称 (回车默认: 宋体): " 
if not defined CHINESE_FONT set "CHINESE_FONT=宋体"

set "WESTERN_FONT="
set /p "WESTERN_FONT=请输入西文字体名称 (回车默认: Times New Roman): " 
if not defined WESTERN_FONT set "WESTERN_FONT=Times New Roman"

echo.
echo ========================================================
echo 支持直接输入数字 (如 10.5, 12) 或标准中文字号名称： 
echo 初号=42, 小初=36, 一号=26, 小一=24, 二号=22, 小二=18 
echo 三号=16, 小三=15, 四号=14, 小四=12, 五号=10.5, 小五=9 
echo ========================================================
set "FONT_SIZE_INPUT="
set /p "FONT_SIZE_INPUT=请输入字号大小 (回车默认: 10): " 
if not defined FONT_SIZE_INPUT set "FONT_SIZE_INPUT=10"

:: 2. 通过系统环境变量将参数传递给 PowerShell，彻底杜绝字符串引号冲突引起的报错
set "WORD_FONT_ZH=%CHINESE_FONT%"
set "WORD_FONT_EN=%WESTERN_FONT%"
set "WORD_FONT_SIZE=%FONT_SIZE_INPUT%"

echo.
echo ========================================================
echo  正在处理，请稍候... 
echo ========================================================
echo.

:: 强行关闭后台运行的 winword 进程，防止文件被锁定
taskkill /f /im winword.exe >nul 2>&1

:: 运行 PowerShell 生成 master 模板并分发 (内置字号映射字典与错误捕获机制)
powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference = 'Stop'; try { Write-Host '1. 正在启动 Word 生成标准模板并更新 Normal.dotm...' -ForegroundColor Cyan; $sizeMap = @{ '初号'=42; '小初'=36; '一号'=26; '小一'=24; '二号'=22; '小二'=18; '三号'=16; '小三'=15; '四号'=14; '小四'=12; '五号'=10.5; '小五'=9; '六号'=7.5; '小六'=6.5; '七号'=5.5; '八号'=5 }; $fontSize = 10; if ($sizeMap.ContainsKey($env:WORD_FONT_SIZE)) { $fontSize = $sizeMap[$env:WORD_FONT_SIZE] } elseif ($env:WORD_FONT_SIZE -match '^\d+(\.\d+)?$') { $fontSize = [float]$env:WORD_FONT_SIZE } else { Write-Host '字号输入有误，自动使用默认值 10' -ForegroundColor Yellow }; $word = New-Object -ComObject Word.Application; $word.Visible = $false; $word.DisplayAlerts = 0; $doc = $word.Documents.Add(); $selection = $word.Selection; $selection.Font.Name = $env:WORD_FONT_EN; $selection.Font.NameAscii = $env:WORD_FONT_EN; $selection.Font.NameFarEast = $env:WORD_FONT_ZH; $selection.Font.Size = $fontSize; $selection.Font.SetAsTemplateDefault(); $word.NormalTemplate.Save(); $style = $doc.Styles.Item(-1); $style.Font.Name = $env:WORD_FONT_EN; $style.Font.NameAscii = $env:WORD_FONT_EN; $style.Font.NameFarEast = $env:WORD_FONT_ZH; $style.Font.Size = $fontSize; $tempTemplate = \"$env:TEMP\WORD_MASTER_TEMPLATE.DOCX\"; $doc.SaveAs($tempTemplate); $doc.Close(0); $word.Quit(); [System.Runtime.Interopservices.Marshal]::ReleaseComObject($word) | Out-Null; Write-Host '2. 正在强行同步模板至 Windows 及 Office VFS 虚拟化目录...' -ForegroundColor Cyan; $targetFolders = @(\"$env:SystemRoot\SHELLNEW\", \"${env:ProgramFiles}\Microsoft Office\root\VFS\Windows\SHELLNEW\", \"${env:ProgramFiles(x86)}\Microsoft Office\root\VFS\Windows\SHELLNEW\"); foreach ($folder in $targetFolders) { if ($folder -and (Test-Path (Split-Path $folder -Parent))) { if (!(Test-Path $folder)) { New-Item -ItemType Directory -Path $folder -Force | Out-Null }; Copy-Item -Path $tempTemplate -Destination \"$folder\WORD12.DOCX\" -Force -ErrorAction SilentlyContinue; Copy-Item -Path $tempTemplate -Destination \"$folder\WORD.DOCX\" -Force -ErrorAction SilentlyContinue; Write-Host \"   已同步更新: $folder\" -ForegroundColor Gray; } }; Remove-Item -Path $tempTemplate -Force -ErrorAction SilentlyContinue; Write-Host '模板分发成功！' -ForegroundColor Green; } catch { Write-Host '处理失败：' $_.Exception.Message -ForegroundColor Red; exit 1; }"

if %errorlevel% neq 0 (
    echo.
    echo 【处理失败】未能成功生成或分发模板文件，请确保已安装 Microsoft Word。 
    pause
    exit /b
)

echo 3. 正在更新注册表右键新建项指向... 

:: 关联注册表 ShellNew 路径指向 WORD12.DOCX
reg delete "HKEY_CLASSES_ROOT\.docx\ShellNew" /v "NullFile" /f >nul 2>&1
reg add "HKEY_CLASSES_ROOT\.docx\ShellNew" /v "FileName" /t REG_SZ /d "WORD12.DOCX" /f >nul 2>&1

reg delete "HKEY_CLASSES_ROOT\.docx\Word.Document.12\ShellNew" /v "NullFile" /f >nul 2>&1
reg add "HKEY_CLASSES_ROOT\.docx\Word.Document.12\ShellNew" /v "FileName" /t REG_SZ /d "WORD12.DOCX" /f >nul 2>&1

reg delete "HKEY_CLASSES_ROOT\Word.Document.12\ShellNew" /v "NullFile" /f >nul 2>&1
reg add "HKEY_CLASSES_ROOT\Word.Document.12\ShellNew" /v "FileName" /t REG_SZ /d "WORD12.DOCX" /f >nul 2>&1

echo.
echo ========================================================
echo 【完全成功】优化已完成！ 
echo 无论未来重置注册表、升级 Office 还是使用任何方式新建， 
echo Word 的默认字体均已牢牢固定为您配置的字体和字号。 
echo ========================================================
echo.
pause
```
