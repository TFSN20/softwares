1. mklink 文档 下载 图片 视频 到 D盘
2. 创建还原点
- 显示器，选择仅仅显示2屏幕，1600*900 60hz，

## 公司电脑
- 关闭最近使用的文件，常用的文件夹，设置里关闭搜索建议和云搜索

## 系统禁止更新
- 家庭版启用策略
  ```
  @echo off
  pushd "%~dp0"
  dir /b %systemroot%\Windows\servicing\Packages\Microsoft-Windows-GroupPolicy-ClientExtensions-Package~3*.mum >gp.txt
  dir /b %systemroot%\servicing\Packages\Microsoft-Windows-GroupPolicy-ClientTools-Package~3*.mum >>gp.txt
  for /f %%i in ('findstr /i . gp.txt 2^>nul') do dism /online /norestart /add-package:"%systemroot%\servicing\Packages\%%i"
  pause
  ```
- win+r
  ```
  gpedit.msc
  ```
- 依次进入
  ```
  计算机配置
  → 管理模板
  → Windows 组件
  → Windows 更新
  → 管理最终用户体验
  ```
  配置自动更新改为禁用
- 注册表方法(保存到`2099.reg`双击运行)
  ```
  Windows Registry Editor Version 5.00
  
  [HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\WindowsUpdate\UX\Settings]
  "FlightSettingsMaxPauseDays"=dword:00001b58
  "PauseFeatureUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseFeatureUpdatesEndTime"="2099-12-31T23:59:59Z"
  "PauseQualityUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseQualityUpdatesEndTime"="2099-12-31T23:59:59Z"
  "PauseUpdatesStartTime"="2026-01-01T00:00:00Z"
  "PauseUpdatesExpiryTime"="2099-12-31T23:59:59Z"
  ```
## 鼠标定位圆圈
保存为`鼠标定位圆圈.bat`双击运行
```
@echo off
:: 设置编码为 UTF-8 防止中文乱码
chcp 65001 >nul
cls

echo ==================================================
echo   Windows 鼠标定位设置（按下 Ctrl 显示圆圈）
echo ==================================================
echo   [1] 开启该功能
echo   [2] 关闭该功能 
echo   [3] 退出
echo ==================================================
set /p choice="请输入数字 (1-3) 并按回车: "

if "%choice%"=="1" goto ENABLE
if "%choice%"=="2" goto DISABLE
if "%choice%"=="3" goto END
goto INVALID

:ENABLE
set "status=$true"
set "msg=已成功【启用】按下 Ctrl 键显示鼠标位置功能！"
goto PROCESS

:DISABLE
set "status=$false"
set "msg=已成功【关闭】按下 Ctrl 键显示鼠标位置功能！"
goto PROCESS

:PROCESS
:: 创建临时 PowerShell 脚本以规避批处理转义字符问题
set "temp_ps=%temp%\sonar_settings.ps1"
echo $code = @' > "%temp_ps%"
echo using System.Runtime.InteropServices; >> "%temp_ps%"
echo public class MouseSonar { >> "%temp_ps%"
echo     [DllImport("user32.dll")] >> "%temp_ps%"
echo     public static extern int SystemParametersInfo(uint u, uint up, bool lpv, uint fu); >> "%temp_ps%"
echo } >> "%temp_ps%"
echo '@ >> "%temp_ps%"
echo Add-Type -TypeDefinition $code >> "%temp_ps%"
echo [MouseSonar]::SystemParametersInfo(0x101D, 0, %status%, 3) >> "%temp_ps%"

:: 执行并清理临时文件
powershell -NoProfile -ExecutionPolicy Bypass -File "%temp_ps%"
del "%temp_ps%"

echo.
echo %msg%
echo.
pause
goto END

:INVALID
echo 输入无效，请重新运行脚本。
pause
goto END

:END
exit
```
## 设置网络DNS服务器地址
保存为`SetDNS.bat`双击运行
```
@echo off
:: 设置编码为 UTF-8 防止中文乱码
chcp 65001 >nul
cls
setlocal enabledelayedexpansion

:: 检查并自动获取管理员权限
>nul 2>&1 "%SYSTEMROOT%\system32\cacls.exe" "%SYSTEMROOT%\system32\config\system"
if '%errorlevel%' NEQ '0' (
    echo 正在请求管理员权限...
    goto UACPrompt
) else ( goto gotAdmin )

:UACPrompt
    echo Set UAC = CreateObject^("Shell.Application"^) > "%temp%\getadmin.vbs"
    echo UAC.ShellExecute "%~s0", "", "", "runas", 1 >> "%temp%\getadmin.vbs"
    "%temp%\getadmin.vbs"
    exit /B

:gotAdmin
    if exist "%temp%\getadmin.vbs" ( del "%temp%\getadmin.vbs" )
    pushd "%CD%"
    CD /D "%~dp0"

:menu
cls
echo ===================================================
echo             Windows 一键设置 DNS 工具
echo ===================================================
echo  [1] 阿里云 DNS (223.5.5.5 / 223.6.6.6)
echo  [2] 腾讯云 DNS (119.29.29.29 / 119.28.28.28)
echo  [3] 百度云 DNS (180.76.76.76 / 180.76.76.77)
echo  [4] 114 DNS (114.114.114.114 / 114.114.115.115)
echo  [5] 谷歌 DNS (8.8.8.8 / 8.8.4.4)
echo  [6] Cloudflare DNS (1.1.1.1 / 1.0.0.1)
echo  [7] 手动输入自定义 DNS
echo  [8] 恢复自动获取 DNS (DHCP)
echo  [9] 退出
echo ===================================================
set /p choice="请输入选项 [1-9]: "

if "%choice%"=="1" (
    set "DNS1=223.5.5.5"
    set "DNS2=223.6.6.6"
    goto SelectAdapter
)
if "%choice%"=="2" (
    set "DNS1=119.29.29.29"
    set "DNS2=119.28.28.28"
    goto SelectAdapter
)
if "%choice%"=="3" (
    set "DNS1=180.76.76.76"
    set "DNS2=180.76.76.77"
    goto SelectAdapter
)
if "%choice%"=="4" (
    set "DNS1=114.114.114.114"
    set "DNS2=114.114.115.115"
    goto SelectAdapter
)
if "%choice%"=="5" (
    set "DNS1=8.8.8.8"
    set "DNS2=8.8.4.4"
    goto SelectAdapter
)
if "%choice%"=="6" (
    set "DNS1=1.1.1.1"
    set "DNS2=1.0.0.1"
    goto SelectAdapter
)
if "%choice%"=="7" (
    set /p DNS1="请输入首选 DNS: "
    set /p DNS2="请输入备用 DNS (若无备用请直接回车): "
    goto SelectAdapter
)
if "%choice%"=="8" (
    goto SelectAdapter
)
if "%choice%"=="9" (
    exit
)
echo 输入错误，请重新输入！
pause
goto menu

:SelectAdapter
cls
echo 正在扫描并分析系统中的网卡...
set "count=0"

:: 1. 扫描并优先添加：已连接的物理网卡 (推荐级最高)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    if /I "!state!"=="connected" (
        echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            set "adapter_state[!count!]=已连接"
        )
    )
)

:: 2. 扫描并添加：未连接的物理网卡 (推荐级次之)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    if /I "!state!"=="disconnected" (
        echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            set "adapter_state[!count!]=未连接"
        )
    )
)

:: 3. 扫描并添加：虚拟网卡 (推荐级最低)
for /f "skip=2 tokens=4*" %%i in ('netsh interface ipv4 show interfaces') do (
    set "state=%%i"
    set "name=%%j"
    for /f "tokens=* delims= " %%a in ("!name!") do set "name=%%a"
    
    echo !name! | findstr /I /C:"Loopback" /C:"vEthernet" /C:"Virtual" /C:"VMware" /C:"VirtualBox" >nul
    if !errorlevel! equ 0 (
        :: 过滤掉没有修改价值的 Loopback 网卡
        echo !name! | findstr /I /C:"Loopback" >nul
        if !errorlevel! equ 1 (
            set /a count+=1
            set "adapter[!count!]=!name!"
            if /I "!state!"=="connected" (
                set "adapter_state[!count!]=已连接 (虚拟)"
            ) else (
                set "adapter_state[!count!]=未连接 (虚拟)"
            )
        )
    )
)

if "%count%"=="0" (
    echo [错误] 未在系统中检测到有效的网络适配器！
    pause
    goto menu
)

:ShowChoice
cls
echo ===================================================
echo                请选择要应用 DNS 的网卡
echo ===================================================
echo  [A] 应用到所有当前“已连接”的网卡 (推荐)
echo ---------------------------------------------------
for /L %%g in (1,1,%count%) do (
    echo  [%%g] !adapter[%%g]!  ^(!adapter_state[%%g]!^)
)
echo ===================================================
set /p adapter_choice="请输入选项 [A 或数字 1-%count%]: "

if /I "%adapter_choice%"=="A" (
    set "apply_mode=all_active"
    goto ExecuteApply
)

if not defined adapter[%adapter_choice%] (
    echo [错误] 输入的选择无效，按任意键重新选择...
    pause >nul
    goto ShowChoice
)

set "apply_mode=single"
set "selected_adapter=!adapter[%adapter_choice%]!"
goto ExecuteApply

:ExecuteApply
cls
echo 正在配置 DNS，请稍候...
echo.

if "%apply_mode%"=="single" (
    call :SetDNS "%selected_adapter%"
) else (
    set "applied_any=0"
    for /L %%g in (1,1,%count%) do (
        :: 仅对状态为“已连接”的网卡进行操作
        if "!adapter_state[%%g]!"=="已连接" (
            call :SetDNS "!adapter[%%g]!"
            set "applied_any=1"
        )
    )
    if "!applied_any!"=="0" (
        echo [提示] 未发现任何处于“已连接”状态的网卡，无法批量应用。
    )
)

echo.
echo 正在刷新 DNS 缓存...
ipconfig /flushdns
echo.
echo DNS 配置流程已结束。
pause
goto menu

:SetDNS
set "adapterName=%~1"
echo 正在配置网卡 [%adapterName%]...

if "%choice%"=="8" (
    netsh interface ipv4 set dnsservers name="%adapterName%" source=dhcp >nul 2>&1
    if !errorlevel! equ 0 (
        echo [成功] 网卡 [%adapterName%] 已恢复为自动获取 DNS。
    ) else (
        echo [失败] 网卡 [%adapterName%] 恢复失败，请检查名称是否包含特殊字符。
    )
) else (
    netsh interface ipv4 set dnsservers name="%adapterName%" source=static address=%DNS1% register=primary >nul 2>&1
    if !errorlevel! equ 0 (
        echo [成功] 网卡 [%adapterName%] 首选 DNS 已设置为: %DNS1%
        if not "%DNS2%"=="" (
            netsh interface ipv4 add dnsservers name="%adapterName%" address=%DNS2% index=2 >nul 2>&1
            echo [成功] 网卡 [%adapterName%] 备用 DNS 已设置为: %DNS2%
        )
    ) else (
        echo [失败] 网卡 [%adapterName%] DNS 设置失败。
    )
)
goto :eof
```
## 关闭 了解有关此图片的信息 桌面图标
保存为`隐藏图标.bat`双击运行
```
@echo off
:: 使用UTF-8编码，防止CMD窗口中文显示乱码
chcp 65001 >nul

echo 正在修改注册表以隐藏“了解有关此图片的信息”图标...
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\HideDesktopIcons\NewStartPanel" /v "{2cc5ca98-6485-489a-920e-b3e88a6ccce3}" /t REG_DWORD /d 1 /f

echo 正在重启资源管理器使设置生效...
taskkill /f /im explorer.exe
start explorer.exe

echo 隐藏操作已完成！
pause
```
